Lis `CLAUDE.md` et `AGENTS.md`.

Ajoute une section `## Modèles et niveaux d'effort` dans `CLAUDE.md`, à la suite
de `@AGENTS.md` (sans toucher au reste). Cette section doit servir de référence
à toute personne (humaine ou IA) qui contribue au projet.

Elle doit contenir :
- Un modèle par défaut explicite pour le projet, avec justification.
- Une grille « type de tâche → modèle conseillé → pourquoi » couvrant au moins
  3 types de tâches récurrentes sur un projet Next.js + Vitest comme celui-ci.
- Les 3 modèles de la famille Claude actuelle (Opus 4.7, Sonnet 4.6, Haiku 4.5).
- Au moins un mécanisme pour changer de modèle ad-hoc — au minimum l'un de
  `/model` (en session), `--model` (au lancement), ou la clé `model` dans
  `settings.json`.
- Une ligne sur l'extended thinking (toggle `Opt+T` / `Alt+T`, plafond
  `MAX_THINKING_TOKENS`).

Termine en m'expliquant en 2 lignes pourquoi tu as choisi tel modèle par défaut.

Quand c'est fait, exécute `npm run check:2.1` et corrige si besoin.
